import { NotifcationLogoChangePipe } from './notifcation-logo-change.pipe';

describe('NotifcationLogoChangePipe', () => {
  it('create an instance', () => {
    const pipe = new NotifcationLogoChangePipe();
    expect(pipe).toBeTruthy();
  });
});
